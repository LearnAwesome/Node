{
  "name": "09_model-sequelize",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "predev": "cross-env NODE_ENV=production node --use_strict init-db.js",
    "dev": "cross-env NODE_ENV=production node --use_strict app.js",
    "pretest": "cross-env NODE_ENV=test node --use_strict init-db.js",
    "test": "cross-env NODE_ENV=test node --use_strict app.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cross-env": "^5.1.3",
    "mysql2": "^1.5.1",
    "sequelize": "^4.29.2"
  }
}
